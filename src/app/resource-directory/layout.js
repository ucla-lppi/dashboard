// Layout for the Resource Directory section to provide page metadata
export function generateMetadata() {
  return {
    title: 'Resource Directory'
  };
}

export default function ResourceDirectoryLayout({ children }) {
  return <>{children}</>;
}
